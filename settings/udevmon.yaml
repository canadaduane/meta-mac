- JOB: >
    intercept -g $DEVNODE
    | swap_super_alt -l
    | meta_mac
    | caps2esc -m 1
    | uinput -c /etc/interception/clipboard_keys.yaml -d $DEVNODE
  DEVICE:
    LINK: /dev/input/by-path/platform-i8042-serio-0-event-kbd
